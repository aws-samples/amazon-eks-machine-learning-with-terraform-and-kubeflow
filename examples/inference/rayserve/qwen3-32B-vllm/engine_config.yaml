image: public.ecr.aws/docker/library/python:slim-bullseye
inline_script:
- |+
  cat > /tmp/engine.json <<EOF
  {
    "model": "/fsx/pretrained-models/Qwen/Qwen3-32B",
    "tokenizer": "/fsx/pretrained-models/Qwen/Qwen3-32B",
    "enable_log_requests": false,
    "tensor_parallel_size": 8
  }

  EOF
pre_script:
  - mkdir -p $CONFIG_ROOT
  - cp /tmp/engine.json $CONFIG_ROOT/engine.json
process:
  env:
    - name: CONFIG_ROOT
      value: /efs/home/{{ .Release.Name }}/config